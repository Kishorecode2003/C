#include <stdio.h>
#include<math.h>
int main() {
    float n;
    printf("Enter the number in decimals: ");
    scanf("%f",&n);
    int x=floor(n);
    int y=ceil(n);
    printf("%d\n%d",x,y);
    return 0;
}
