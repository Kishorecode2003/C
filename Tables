#include <stdio.h>
#include<math.h>
int main() {
    int n,res;
    printf("Enter the number: ");
    scanf("%d",&n);
    for(int i=1;i<=10;i++)
    {
        res=n*i;
        printf("%d * %d = %d\n",n,i,res);
    }
    return 0;
}
