#include <stdio.h>
#include<string.h>
int main() {
    char str[50];
    printf("Enter the String:");
    scanf("%s",&str);
    int n=strlen(str);
    for(int i=0;i<n;i++)
    {
        printf("%c\n",str[i]);
    }
    return 0;
}
